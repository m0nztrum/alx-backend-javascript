/*
 * Display the message in the console STDOUT
 * @param {string} message - The message to be displayed
 * @return {string} returns a string value
 */

function displayMessage(message) {
  console.log(message);
}

module.exports = displayMessage;
